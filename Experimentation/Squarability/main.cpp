#include <vector>
#include <string>
#include <bits/stdc++.h>
#include "simplex.h"

using namespace std;

int main(){
	int n;
	scanf("%d", n);
	vector<int[4]> recs;
	for (auto& r : recs) {
		r = { 0 };
	}

	for (auto& r : recs) {
		scanf("%d" "%d" "%d" "%d", r[0], r[1], r[2], r[3]);
	}
}